## 1. Planning

- [x] 1.1 Run `pnpm run verify:change` before implementation.
- [x] 1.2 Run `pnpm run task:kickoff` for the owned API Connections UI and OpenSpec paths.

## 2. UI Placement

- [x] 2.1 Add the ISO3 country-code lookup as a built-in Resources row on the API Connections page.
- [x] 2.2 Keep captured API-run resources and empty captured-resource messaging intact.

## 3. Tests and Verification

- [x] 3.1 Update API Connections client tests for the built-in Resources row.
- [x] 3.2 Run direct component tests.
- [x] 3.3 Run `pnpm run verify:change` and complete every required command including `pnpm run verify:change:run`.
- [x] 3.4 Archive the OpenSpec change after verification passes.

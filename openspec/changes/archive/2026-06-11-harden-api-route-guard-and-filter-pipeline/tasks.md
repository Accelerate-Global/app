## Tasks

- [x] Add OpenSpec coverage for centralized API route security and canonical saved-table download filtering.
- [x] Fix `withRoute` so identity-resolution failures are logged and returned as normalized JSON 500 responses.
- [x] Clean formatting of route wrappers introduced by the API guard refactor without changing route behavior.
- [x] Update same-stem tests listed by `verify:test-delta` with meaningful route/component/lib assertions.
- [x] Run required local verification and archive the OpenSpec change after all checks pass.

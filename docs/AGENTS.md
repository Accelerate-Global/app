# Docs Notes

- Release docs must distinguish repo-controlled gates from manual GitHub/Vercel/Supabase enforcement.
- README and release docs should describe admin bootstrap as a manual environment/provider step, not a local env var.
- Keep the CSP documentation aligned with the actual allowances in `next.config.ts`, especially the inline theme bootstrap and Adobe Typekit exceptions.

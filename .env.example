NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
# Required in Vercel for the daily Supabase heartbeat cron. Use a random
# production secret of at least 16 characters.
CRON_SECRET=
# Server-only Resend API configuration for operational incident email. These
# values are also configured independently as Supabase Edge Function secrets.
RESEND_OPERATIONAL_API_KEY=
OPERATIONAL_ALERT_FROM=
OPERATIONAL_ALERT_RECIPIENT=
OPERATIONAL_ALERT_DETAILS_URL=
# Server-only HMAC secret used to create non-reversible identifiers for repeated
# sign-in failure windows. Use at least 32 random characters.
AUTH_FAILURE_HASH_SECRET=
# Server-only. Required for private dataset and reference-resource artifacts;
# never expose it through a NEXT_PUBLIC_* variable.
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_STORAGE_BUCKET=datasets
GOOGLE_SHEETS_SERVICE_ACCOUNT_EMAIL=
# Escaped "\n" private-key newlines are supported.
GOOGLE_SHEETS_SERVICE_ACCOUNT_PRIVATE_KEY=

# Use a Supabase Postgres connection string. The Supabase pooler URL is preferred
# for deployed serverless functions; include sslmode=require when Supabase provides it.
DATABASE_URL=
# Required for certificate-verified non-local production database connections.
# Store the PEM value as literal newlines or escaped \\n sequences.
DATABASE_SSL_CA=

# Disabled-first private Qwen data chat. This feature is server-only and
# admin-only; do not use NEXT_PUBLIC_* variables for any value below.
PRIVATE_DATA_CHAT_ENABLED=false
# Comma-separated, exact server-side pilot identities. The feature fails closed
# when this list is empty, even for administrators.
PRIVATE_DATA_CHAT_CANARY_EMAILS=
# Dedicated constrained analytics login, not DATABASE_URL or service_role.
ANALYTICS_DATABASE_URL=
# At least 32 random characters; used only to pseudonymize audit identities.
PRIVATE_DATA_CHAT_AUDIT_HMAC_KEY=
# HTTPS origin protected by machine authentication. Do not point this at the
# loopback llama.cpp listener or expose the model API directly.
PRIVATE_QWEN_GATEWAY_URL=
# At least 32 random characters and shared only with the private gateway.
PRIVATE_QWEN_GATEWAY_HMAC_KEY=
PRIVATE_QWEN_CF_ACCESS_CLIENT_ID=
PRIVATE_QWEN_CF_ACCESS_CLIENT_SECRET=
# Local/test-only deterministic inference. Never enable in production.
PRIVATE_QWEN_FAKE=false
